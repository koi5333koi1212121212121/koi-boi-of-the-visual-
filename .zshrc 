
  source "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh"
fiif [ some_condition ]; then
  # some command
fiif [ condition ]; then
    # command
fiif [ condition ]; then
    # command
fi

# If you come from bash you might have to change your $PATH.
# export PATH=$HOME/bin:$HOME/.local/bin:/usr/local/bin:$PATH

# Path to your Oh My Zsh installation.
export ZSH="$HOME/.oh-my-zsh"

# Set name of the theme to load --- if set to "random", it will
# load a random theme each time Oh My Zsh is loaded, in which case,
# to know which specific one was loaded, run: echo $RANDOM_THEME
# See https://github.com/ohmyzsh/ohmyzsh/wiki/Themes
ZSH_THEME="powerlevel10k/powerlevel10k"

# Set list of themes to pick from when loading at random
# Setting this variable when ZSH_THEME=random will cause zsh to load
# Example aliases
# alias zshconfig="mate ~/.zshrc"
# alias ohmyzsh="mate ~/.oh-my-zsh"
alias ll="ls -lah"
alias gs="git status"
alias zshconfig="nano ~/.zshrc"
alias ohmyzsh="nano ~/.oh-my-zsh"
POWERLEVEL9K_DISABLE_CONFIGURATION_WIZARD=trueopen -e ~/.zshrc


# To customize prompt, run `p10k configure` or edit ~/.p10k.zsh.
[[ ! -f ~/.p10k.zsh ]] || source ~/.p10k.zsh
source /opt/homebrew/share/powerlevel10k/powerlevel10k.zsh-theme
export PATH=$PATH
export PATH=$PATH
export PATH=$PATH
export PATH=$PATH:/path/to/directory
export PATH="/opt/homebrew/opt/python@3.10/bin:$PATH"

# Created by `pipx` on 2024-12-13 19:42:09
export PATH="$PATH:/Users/koiboimusic/.local/bin"
export PATH="/opt/homebrew/opt/python@3.10/bin:$HOME/.local/bin:$PATH"
alias python="/opt/homebrew/opt/python@3.10/bin/python3.10"
alias python3="/opt/homebrew/opt/python@3.10/bin/python3.10"
export PATH=$PATH:/Users/koiboimusic/Library/Python/3.13/bin

# >>> conda initialize >>>
# !! Contents within this block are managed by 'conda init' !!
__conda_setup="$('/opt/anaconda3/bin/conda' 'shell.zsh' 'hook' 2> /dev/null)"
if [ $? -eq 0 ]; then
    eval "$__conda_setup"
else
    if [ -f "/opt/anaconda3/etc/profile.d/conda.sh" ]; then
        . "/opt/anaconda3/etc/profile.d/conda.sh"
    else
        export PATH="/opt/anaconda3/bin:$PATH"
    fi
fi
unset __conda_setup
# <<< conda initialize <<<

export PATH="/opt/homebrew/bin:$PATH"
